# 这两种脚本对换行的要求相反：shell 脚本必须 LF（CRLF 会让 shebang 变成
# `#!/usr/bin/env bash\r`，Linux 上报 bad interpreter），Windows 批处理必须 CRLF
# （cmd.exe 按字节解析，LF 会错位）。
*.sh  text eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
